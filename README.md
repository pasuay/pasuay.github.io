# pasuay.github.io

Personal landing page listing my projects. Plain static `index.html` — no build
step. Served at <https://pasuay.github.io/>.

To add a project, copy the commented `.card` block in `index.html` and fill in
the title, description, tags, and links.
